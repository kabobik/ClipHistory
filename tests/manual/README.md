# Manual diagnostics

These scripts are small ad-hoc Qt/X11 process and focus diagnostics. They are
kept outside the project root so normal packaging and source browsing stay tidy.

Run them manually when debugging window focus, popup closing, or process state
behavior.
